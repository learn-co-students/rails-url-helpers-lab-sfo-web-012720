<div>
  <% @students.each do |student| %>
    <div><%= student.first_name + " " + student.last_name %></div>
    <div><p><%= link_to student.to_s, student_path(student) %></p></div>
  <% end %>
</div>